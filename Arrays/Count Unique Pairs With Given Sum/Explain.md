# Q12 — Count Unique Pairs With Given Sum

## Problem

Given an integer array `arr` and a target value `target`, count the number of
**unique value-pairs** whose sum is equal to the target.

A pair should be counted only once even if duplicate elements exist.

### Example

```text
arr = [1, 5, 5, 7, -1, 7]
target = 6
